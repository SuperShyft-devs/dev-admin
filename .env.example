# Public API base URL used by the frontend.
# - Dev (with proxy): /api
# - Production (direct): https://api.yourdomain.com
VITE_API_URL=/api

# Dev-only Vite proxy target for requests sent to /api.
VITE_DEV_PROXY_TARGET=http://localhost:8000
